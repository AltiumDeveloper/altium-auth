<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- Offline-friendly: use the installed runtime patch's on-disk packs. -->
    <TargetLatestRuntimePatch>false</TargetLatestRuntimePatch>
    <UseAppHost>false</UseAppHost>
    <!-- Console runner (no xUnit) so it builds/runs offline without NuGet.
         Pass the vectors.json path as the first program argument. -->
    <NuGetAudit>false</NuGetAudit>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../../src/Altium.Auth/Altium.Auth.csproj" />
  </ItemGroup>

</Project>
