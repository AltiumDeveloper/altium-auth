name: TypeScript CI

# Runs only when the TypeScript library or the shared spec/vectors change.
on:
  push:
    branches: [main]
    paths:
      - "libs/typescript/**"
      - "spec/**"
      - ".github/workflows/typescript-ci.yml"
  pull_request:
    paths:
      - "libs/typescript/**"
      - "spec/**"
      - ".github/workflows/typescript-ci.yml"

defaults:
  run:
    working-directory: libs/typescript

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: [20, 22]
    name: Node ${{ matrix.node }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm
          cache-dependency-path: libs/typescript/package-lock.json

      - run: npm ci

      - run: npm run lint

      - run: npm run typecheck

      - run: npm test

      # Conformance against the shared, language-neutral vectors in spec/.
      - run: npm run test:conformance

      - run: npm run build
